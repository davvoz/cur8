import { TempoInMinutiPipe } from './tempo-in-minuti.pipe';

describe('TempoInMinutiPipe', () => {
  it('create an instance', () => {
    const pipe = new TempoInMinutiPipe();
    expect(pipe).toBeTruthy();
  });
});
