import { RanzaAllinteroPipe } from './ranza-allintero.pipe';

describe('RanzaAllinteroPipe', () => {
  it('create an instance', () => {
    const pipe = new RanzaAllinteroPipe();
    expect(pipe).toBeTruthy();
  });
});
