import { ReversePadZeroPipe } from './reverse-pad-zero.pipe';

describe('ReversePadZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new ReversePadZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
