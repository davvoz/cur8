import { InterpretaHTMLDirective } from './interpreta-html.directive';

describe('InterpretaHTMLDirective', () => {
  it('should create an instance', () => {
    const directive = new InterpretaHTMLDirective();
    expect(directive).toBeTruthy();
  });
});
