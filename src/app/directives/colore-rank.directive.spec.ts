import { ColoreRankDirective } from './colore-rank.directive';

describe('ColoreRankDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoreRankDirective();
    expect(directive).toBeTruthy();
  });
});
